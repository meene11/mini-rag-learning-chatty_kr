[project]
name = "mini-rag"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "chromadb>=1.5.9",
    "google-genai>=2.6.0",
    "python-dotenv>=1.2.2",
    "sentence-transformers>=5.5.1",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
